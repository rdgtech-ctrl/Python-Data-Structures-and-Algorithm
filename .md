### Git Commands
```
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/rdgtech-ctrl/Python-Data-Structures-and-Algorithm.git
git branch -M main
git push -u origin main
```

In version control the code is written on cloud than you local computer 

and you sync files to your local computer

multiple people contribute on the code file on cloud

best version control system is Git

Git hosted github very popular website to host your code


Github is a website that allows you to store your code on the cloud. It is not just a cloud storage but a full version control system powered by git.